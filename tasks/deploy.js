const chalk = require('chalk');

/**
 * Deploys the Juice V2 contracts.
 *
 * Example usage:
 * npx hardhat deployJuiceV2 \
 *   --network rinkeby
 */
task('deployJuiceV2', 'Deploys the Juice V1 contracts').setAction(async (taskArgs) => {
  // TODO
});
